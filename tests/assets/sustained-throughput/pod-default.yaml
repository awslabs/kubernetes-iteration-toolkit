apiVersion: v1
kind: Pod
metadata:
  generateName: pod-churn-
  labels:
    group: {{.Group}}  
spec:
  containers:
  - image: registry.k8s.io/pause:3.9
    name: pause
    resources:
      requests:
        cpu: "1m"
        memory: "8Mi"
      limits:
        cpu: "5m"
        memory: "16Mi"
